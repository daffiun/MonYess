import { PlaceholderPage } from '../components/PlaceholderPage';
import { PieChart } from 'lucide-react';

export default function Budgets() {
  return <PlaceholderPage title="Anggaran (Budget)" icon={PieChart} description="Atur batasan pengeluaran bulanan agar tidak jebol." />;
}
